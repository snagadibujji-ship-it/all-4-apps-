kind = "web"
previewPath = "/"
title = "LocalMart — Shop Local"
version = "1.0.0"
id = "artifacts/customer-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/customer-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/customer-app", "run", "build" ]
publicDir = "artifacts/customer-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
